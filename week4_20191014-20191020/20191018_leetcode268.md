``` java
class Solution {
    public int missingNumber(int[] nums) {
        int sum = (nums.length * nums.length + nums.length) / 2;
        for(int num: nums){
            sum -= num;
        }
        return sum;
    }
}
```