```java
class Solution {
    public boolean isOneBitCharacter(int[] bits) {
        int strStart = 0;
        while(strStart < bits.length - 1){
            int increment = bits[strStart] == 1? 2:1;
            strStart += increment;
        }
        return strStart == bits.length - 1;
    }
}
```

